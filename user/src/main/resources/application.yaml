server:
  port: 5000

spring:
  application:
    name: TUBE_PLUS

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 500
      #        show_sql: true
      dialect: org.hibernate.dialect.MySQL5Dialect
    open-in-view: true # false ?? ??? ??

  datasource:
    url: jdbc:mysql://34.64.88.166:3306/tubeplus
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

